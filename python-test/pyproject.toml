[tool.poetry]
name = "python-test"
version = "0.1.0"
description = ""
authors = ["Kevin Moses <kevin@semgrep.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
werkzeug = "3.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
